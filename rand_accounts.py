import random
import string

def gen_rand_id(choices, ilen):
    return ''.join((random.choice(choices) for _ in range(ilen)))

def gen_random_user(seed_nb):

    random.seed(seed_nb)


    random.choice(choices)
    string.ascii_letters
    string.digits
    print(random.random()) #double
    #for age INTEGER field ran INTEGER between 18-100
    #for name len 5-15 ch/nb
    #sexual  1-3 for 1straight, ....
    """

    

def gen_N_random_user(seed_id, N)
